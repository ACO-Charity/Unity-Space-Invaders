using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Introduction : MonoBehaviour
{
    //Klassen dürfen nicht in Methoden definiert werden, also musst du sie außerhalb von Start() schreiben!

    //Code, den du in Start() schreibst, wird ausgeführt wenn du in Unity auf "Play" drückst
    void Start()
    {
    }
}
